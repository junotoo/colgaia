Module f23ex1
    Sub Main(args As String())

        Dim max As Integer = pedirInteger()
        Dim max As Integer = pedirInteger()
        Dim min As Integer

        Integer =
        Do

        Loop Until

    End Sub

End Module
